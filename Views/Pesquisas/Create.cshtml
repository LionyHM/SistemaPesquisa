@model SistemaPesquisa.Models.Pesquisa

@{
    ViewData["Title"] = "Create";
}

<div class="d-flex flex-grow-1 justify-content-center align-items-center">
    <div class="">
        <h4 class="@ViewBag.StyleMessage">@ViewBag.ErrorMessage</h4>
        <h4 class="@ViewBag.StyleMessage">@ViewBag.SuccessMessage</h4>
    </div>
</div>
<hr />


<div>
    <a asp-action="Index">Lista de pesquisas</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
